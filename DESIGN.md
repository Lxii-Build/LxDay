# 当前产品与交互基线

本文只记录当前仍有效的产品决策。实现细节和硬性约束以 [AGENTS.md](AGENTS.md) 为准，接口以 [server/README.md](server/README.md) 和 [docs/ALBUM.md](docs/ALBUM.md) 为准。

## 产品范围

林曦日记是两个人使用的 Android 应用，核心是状态共享、轻量互动、待办和共同相册。账号通过邮箱注册/登录，登录后用一次性邀请码建立关系。关系数据和业务数据按 `pair_id` 隔离。

当前支持：

- 此刻状态、状态历史和电量曲线；
- 求陪伴、求冷静、响铃提醒，并支持撤回和冷却；
- 双向待办、重复提醒、强提醒和本地闹钟兜底；
- 相册分组、批量上传、分页、左右滑动查看、评论、点赞和回收站；
- 一起听：每对情侣一个活动房间，已绑定伴侣可直接进入；房间只同步播放元数据，
  音频继续由各自手机上的播放器负责；
- 正式版/prerelease 更新检查、历史 `CHANGELOG.md` 展示和 APK 下载。

明确不做：iOS 客户端、商业推送、把共享通讯密钥放进 APK、公开私密相册原图、依赖客户端隐藏入口实现权限控制。

## 导航与返回

主界面由“此刻、待办、发现、我的”组成。相册和设置等属于二级页面，进入和返回都走统一导航状态机；返回手势/系统返回必须回到上一级，而不是退出 Activity。页面切换使用统一的横向退场/入场动画，方向由导航栈决定，不能在单个页面自行写一套动画。

发现页进入相册后，返回应表现为“相册向左退出、发现页从左侧恢复”的连续过渡；不能闪现、缩放或直接切回主界面。动画必须尊重系统“减少动态效果”设置。

## 弹窗与操作

- 取消按钮固定在左侧，确认/更新按钮固定在右侧；两者背景色和语义必须不同。
- 危险操作使用红色确认按钮，并在弹窗出现后延迟一秒可确认；请求进行中不可通过外部关闭制造“像是取消”的错觉。
- 更新弹窗把可滚动的日志正文和底部按钮分开：正文可滚动，按钮不随正文滚动，也不需要滑到正文底部才出现。
- 无可用更新时，更新按钮保持不可用；有更新时，右侧按钮执行下载，左侧按钮关闭弹窗。
- 面向用户的文字使用中文，错误态必须提供重试动作；空数据和网络错误不能使用同一文案。

## 状态共享

用户默认开启状态共享。首次绑定仍需完成双方知情授权；授权已存在时，启动服务直接恢复采集，不要求用户重复点击设置。总开关关闭后停止采集、清理本地待上传状态并阻断服务端写入；服务端开关校验不能依赖客户端入口。

采集包括电量、充电、屏幕、前台应用、网络和音乐通知等可用信息。权限被拒只影响对应字段，不能让整个应用无法使用。WebSocket 用于实时同步，REST 上报和本地 AlarmManager 用于降级。

## 相册体验

- 网格缩略图始终有底色和加载/失败占位，不能把加载失败渲染成透明空白。
- 多张照片查看使用水平 Pager，支持滑动、双指缩放和返回；当前照片位置要明确。
- 列表分页追加必须按稳定 ID 去重，不能把“已加载条数”当作永远稳定的数据库 offset。
- 上传前限制像素、帧数、尺寸和内存；服务端再次校验，GIF/大图不能通过一次解码把进程内存打满。
- 私密媒体只通过鉴权 `/media/<id>` 读取，后台审核只给 384px 缩略图。

## 后台定位

后台只保留能实际改变或审计系统行为的页面：看板、用户、关系、相册/内容审核、APP 版本、通知、存储、运行参数、审计和网络日志。所有编辑、删除、封禁、解绑、恢复和清理都必须有可见操作、服务端权限校验和结果反馈。

后台设置不是死常量：可调参数必须登记到 `server/settings.go` 的规格表，并由读取端使用同一缓存/数据库来源。涉及不可逆删除或安全强度的参数只允许超级管理员。

## 安全原则

1. APK 可以被逆向，因此不信任 APK 内的秘密；当前客户端只使用 HTTPS/WSS 和用户 JWT。
2. JWT 固定 HS256，令牌版本可撤销；封禁账号后 REST 和 WebSocket 都立即失效。
3. Android 令牌使用 Keystore AES/GCM 保存；旧版明文只做一次安全迁移，无法加密就清除。
4. 所有输入在服务端做长度、格式、范围、归属和状态校验；认证失败不泄露账号是否存在。
5. 上传、图片解码、网络请求、SMTP、WebSocket 写入都有资源或时间上限。
6. 日志与审计采用默认脱敏；任何私密照片路径、认证头和凭据都不能进入后台可读日志。

## 视觉语言

Android 使用 miuix 组件和 `MiuixIcons`，卡片圆角 16dp、按钮 16dp、缩略图 8dp，触达区域至少 48dp。Material 只保留在 `ui/theme` 的配色计算兼容层。后台沿用 Element Plus，但业务页优先使用清晰的状态、危险操作颜色、空态和错误态。
