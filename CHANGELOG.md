# 更新日志 · Changelog

本项目所有值得记录的版本变更都记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（`vMAJOR.MINOR.PATCH`）。

> 约定：发行版由「发行版工作流」手动触发，`versionCode` 由 tag 推导。
> 面向用户的应用介绍见 [docs/APP_INTRO.md](docs/APP_INTRO.md)，此后每个版本在此追加一条更新日志。

## [Unreleased]

（暂无）

## [1.0.9] - 2026-09-02

> 本版本以 GitHub prerelease 发布，供测试验证；不会触发强制更新。

### 更新与发布

- APP 更新源改为 GitHub Releases，正式版与测试版分渠道读取，并自动识别 APK 下载链接。
- 更新弹窗展示新版本号、发布时间、版本渠道、更新说明与历代 Changelog；没有新版本时也可查看历史日志。
- 关于页移除官网入口，改为更新日志；后台 APP 版本页改为 GitHub Release 看板，并展示服务端版本与提交短 SHA。
- 删除旧的服务端 APP 版本管理表与手工发布接口，历史数据通过迁移清理，避免后台与 GitHub 版本信息分裂。

### 后台与服务端

- 用户管理支持按启用/禁用筛选，封禁会立即撤销现有用户令牌；关系管理支持搜索、编辑纪念日、解除绑定与作废挂起邀请。
- 撤回求陪伴/求冷静请求时同步清理离线队列，并取消接收端对应通知，避免撤回后重连又收到旧提醒。
- 服务端构建注入版本号和提交短 SHA，补齐 GitHub Releases 缓存、ETag、频道筛选和 APK 降级链接。
- 不再把可从 APK 提取的 APP_KEY 当作安全边界；Release 包强制 HTTPS/WSS，业务鉴权依赖 JWT、实时状态校验与限流。

### Android 体验与可靠性

- 状态共享默认开启但仍遵守首次隐私授权；状态卡片开关与后台采集解耦，避免已开启状态共享却不采集。
- 远程互动按钮支持再次点击及时撤回，更新导航转场与返回动画，并完善通知 ID 隔离、离线失败提示和重连行为。
- 更新日志弹窗支持滚动查看历史版本；相册浏览、图片加载鉴权与缓存继续统一走安全媒体代理。

### 验证

- 后台 `vue-tsc` 与 Vite 生产构建通过；服务端与 Android 编译/测试由发布工作流执行并作为发布门禁。

## [1.0.8] - 2026-09-01

### 运营后台数据维护

- 用户管理新增资料编辑：可维护昵称、邮箱、性别、简介与生日；用户名、头像和登录状态保持独立，避免误改账号身份。
- APP 版本新增编辑入口，可修订版本名、APK 地址、更新说明与强制更新标记；平台与版本号作为发布身份保持不变。
- 绑定关系支持维护纪念日；待办支持编辑内容、提醒对象、提醒时间与重复规则；相册支持改名；照片审核支持修改描述。
- 所有新增写操作统一要求超级管理员、执行字段校验并写入审计日志；邀请码、照片地址及文件内容不会下发到后台表单。

### 验证与回归

- 补充后台编辑存储层回归测试，覆盖可空字段清空、提醒时间清空、关系纪念日与 APP 版本信息落库。
- 后台 `vue-tsc`、Vite 生产构建及本次涉及文件的 ESLint 检查通过。

## [1.0.7] - 2026-08-31

### Android 客户端体验

- 首页伴侣状态改为“同步可信度 + 当前状态 + 电量/屏幕/网络”分层展示，长 Wi‑Fi 名称、应用名与
  音乐信息会安全截断；过期与失联状态同时使用文字和颜色提示，不再挤在一行里难以扫读。
- 远程互动与本机状态补充用途说明，本机四项信息改为紧凑的标签/值布局；互动按钮明确暴露
  进行中与禁用语义，统一按钮组件补齐无障碍 Button 角色。
- 相册页突出“新建相册”主操作，把“这一天”和“回收站”收为次级入口，并增加照片与相册总数概览；
  明确封面的装饰语义，并让管理入口读出对应的相册名称。

### 运营后台体验

- 数据看板重做信息层级：新增运营总览、最后更新时间、可重试错误态、保留旧数据策略、指标说明、
  空趋势反馈与响应式布局；手机单列、平板双列、桌面四列均保持清晰可扫读。
- 登录表单增加可见字段标签与正确的账号/密码自动填充语义；拖动验证支持键盘完成并提供读屏状态。
- 顶栏图标入口改为真实按钮，统一 44px 触达区、焦点轮廓、悬停提示和读屏名称；移动端不再弹出
  会遮住页面内容的首次设置引导，并为动态效果补充“减少动态效果”适配。

### 验证与回归

- 后台移动端审计不再假定页面第一个按钮就是提交按钮，并新增最终路由校验，防止 404 页面被当作
  “非白屏”而假绿；截图前主动触发懒加载并等待图表动画稳定。
- 生产构建、定向 ESLint/Stylelint 与四档真实浏览器视口（412×915、360×640、390×844、
  768×1024）全部通过，覆盖登录及 12 个菜单页。

## [1.0.6] - 2026-08-31

### 安全与可靠性

- WebSocket 上行改为显式事件白名单，拒绝客户端伪造 `todo_new`、`admin_notice` 等仅能由
  服务端在真实写入后产生的事件；互动事件只保留受限的 `ring_id`，不再把任意 JSON 长期放入离线队列。
- 状态上报统一校验电量、网络类型及 SSID/应用/音乐字段长度；Wi‑Fi 通知加入限频，避免异常客户端
  或系统重连刷满伴侣通知与离线补偿队列。
- 低电量提醒改为首次进入阈值或电量继续下降时发送，不再对前台每 10 秒一次的相同状态重复推送。
- 就绪探针从网络日志中排除，避免编排平台的周期性 `/readyz` 请求无意义增长日志表。

### 客户端与后台

- Android REST、下载与上传请求统一在作用域内关闭响应体，避免长期使用后耗尽 OkHttp 连接池；
  移除已无调用方的任意 URL 下载接口。
- Android 所有页面按钮统一接入 `LxButton` 的语义色、圆角和 48dp 最小触达区域；补充内容式组件支持，
  不再遗留裸 miuix Button。
- 低电量事件只由服务端状态落地链路生成，客户端上行消息同样采用白名单，消除重复提醒。
- 后台外链仅允许 HTTP(S)，并以 `noopener,noreferrer` 打开，防止新标签页反向控制后台页面。

## [1.0.5] - 2026-08-31

### 部署、恢复与可靠性

- 新增 `/readyz` 就绪检查：除进程存活外，实际验证 SQLite 可写、公开媒体目录与私密媒体目录可写；
  数据卷只读、磁盘满等故障会明确返回 503，避免被 `/healthz` 的“进程仍活着”掩盖。
- 后台请求日志、数据保留清理、内存缓存清扫与待办提醒扫描纳入统一停机流程，先停止 worker、
  再关闭 SQLite，避免容器退出期间继续访问已关闭数据库。
- 新增不改编排文件的 Docker 卷备份与隔离恢复演练脚本，随附校验清单与操作文档；备份文件采用
  `umask 077` 创建，恢复演练不会接触生产卷。
- 独立服务端镜像也兼容历史 root 属主的数据卷，并在 CI 中覆盖此回归场景。

### 构建、测试与发布

- 新增服务端、后台与 Android 的自动质量工作流：依赖锁定、格式化、静态检查、单元测试、后台类型检查与构建
  与 Android Debug 编译/测试/lint 均进入合并门禁。
- Android lint 改为阻断发布构建；服务端测试增加明确超时，依赖漂移会在 CI 中失败。
- Docker 构建固定使用 `npm ci`、`go mod download` 和 `-mod=readonly`，减少“本机能构建、CI 不可复现”。
- 服务端镜像同时发布 `linux/amd64` 与 `linux/arm64` 清单；镜像 tar 产物与发行版资产附带 SHA-256 校验文件。

### 数据与安全

- SQLite 初始化迁移引入 `schema_migrations` 版本账本，后续升级可按版本追加并阻止新程序误写入更高版本数据库。
- 后台图标组件不再把远程 SVG 文本注入 DOM，改由受浏览器安全模型约束的图片资源加载。

## [1.0.4] - 2026-08-31

### 部署与可靠性

- **修复历史 Docker 数据卷导致 SQLite 只读**：镜像启动时一次性修复旧命名卷的属主，
  随后仍以非 root 用户运行服务；不需要修改 `docker-compose.yml`、`.env` 或现有卷配置。
- 保持 `/app/data`、`/app/uploads` 与 `/app/uploads-private` 的持久化路径和权限边界不变，
  配置文件缺失继续按默认值与环境变量启动。

## [1.0.3] - 2026-08-31

**GIF 帧炸弹的实证修复 + 单张图片最坏内存的下限**。外部报告指出
GIF 帧炸弹（1000 帧 × 1000×1000）未被完全挡住，且现有像素上限导致
单张最坏占用约 256MB × 3 路 = 768MB，足以打死小规格 VPS。

本轮把像素上限从 64M 降到 12M（默认；后台可调，限超管），并揪出图片
缩放的真凶：**缩放自身也吃内存，且与原图尺寸成正比、与目标无关**。
实测把公式钉死了：`目标宽 × 源高 × 32 字节`，8000×8000 → 384 约 95MB。
相册每张跑两次（缩略图 + 预览图），单段走完约 157 MB。

两段式缩放（先廉价缩到目标的 1.25 倍、再高质量收尾）把它压到约 63 MB，
且预览图的成本**是 API 决定的常量**，与原图多大完全无关。
3 路并发闸门的实际封顶现已从 768MB 降到约 330MB。

同时新增三道基于 IP 的限流（注册、发码、登录）与单账号在飞行上限，
挡住 APP_KEY 被逆向后仍可行的放大路径。

### 服务端 · 内存与性能

- **GIF 帧炸弹的实证修复**：v1.0.2 加了结构扫描，但外部实测仍可传入
  1000 帧的炸弹。排查后确认 v1.0.2 的修法完全生效（实测 0.0 MB 就拒绝），
  而报告的样本是用 `git.DecodeAll` **做出来**的 —— 那正是旧实现的路径。
  本轮用报告给的确切参数写了**实证测试**：`TestFrameBombRejectedWithBoundedMemory`
  断言被拒绝且内存增长有上界（64 MB），把实现改回 DecodeAll 时会分配 982 MB。
  这种"只断言错误、不度量内存"的测试完全抓不到 bug —— 两条路径的错误码一模一样。
- **像素上限从 64M 降到 12M（默认；后台可调，限超管）**：64M 像素解成 RGBA
  约 256MB/张，配 3 路并发就是 768MB，足以打死小规格 VPS。而客户端上传前
  一律把长边压到 2048（约 3.1M 像素），所以正常路径离 12M 还差 4 倍、完全碰不到。
  12M 恰好是主流手机主摄的默认直出尺寸（4000×3000），所以「不走客户端、
  直接调接口传原图」这种用法也仍然可用。需要更高时超管可在后台调到 64M。
- **★ 图片缩放自身也吃内存**（最隐蔽的那个）：`draw.CatmullRom` 的 Scale
  会分配一块按**源图高度**成正比的临时缓冲，而不是只与目标尺寸有关。
  实测把公式钉死了：**分配 ≈ 目标宽 × 源高 × 32 字节**。
  所以**目标越大、源图越高，就越贵**，而相册每张要跑两次（缩略图 + 预览图）。
  顶到像素上限的一张图单段走完两次约 157 MB，配 3 路并发最坏约 470 MB。
  而这**与图片格式无关**：一张合法的大 PNG 就能触发，文件不大、帧数为 1、
  所有既有校验都放行，比 GIF 帧炸弹更隐蔽（v1.0.2 的所有内存修复对这条完全不管用）。
  修法是两段式：先用 `ApproxBiLinear`（不分配那块缓冲）廉价缩到目标的 1.25 倍，
  再用 `CatmullRom` 从中间图收尾。公式里的"源高"从原图高度降到了目标高度的 1.25 倍，
  **与原图多大彻底脱钩**。实测缩略图 94.77 MB → 6.60 MB。
  剩下的成本是 API 决定的常量：1080 预览图的第二段需要约 44.8 MB，
  即便 prescaleFactor 取 1（第二段退化成等尺寸重采样、画质白给）也仍需约 35.8 MB。
  所以 1080 预览图的开销压不到更低，除非换掉插值器 —— 那是画质取舍，
  不在本次内存修复的范围内。
- **★ 按 IP 的三道限流（注册、发验证码、登录）**：`APP_KEY` 通过
  `BuildConfig` 编进 APK，任何人 `jadx` 反编译就能拿到明文。
  这不是本项目的实现缺陷 —— **任何随客户端分发的密钥都不可能保密**，
  客户端上没有能藏住秘密的地方。所以 `APP_KEY` 的真实作用只有一个：
  把顺手扫全网的爬虫挡在门外（它们不会为一个私有 App 去逆向），
  它**不构成任何安全边界**。正确的威胁模型是：**假设攻击者已持有合法 APP_KEY**，
  在这个前提下服务端仍然必须不被打死、不被刷爆。
  而在这个前提下，此前所有限流的键**全部由攻击者自己控制**：
  账号名、邮箱、uid 都来自攻击者提供的数据，换一个就从零开始。
  唯一不受攻击者随意支配的维度就是**来源 IP**，而它此前只用于写审计日志、
  从未参与任何限流判定。这一层就是补这个缺口。
  注册 5 次/小时（情侣应用，一个人一辈子注册一次）、发码 10 次/小时、
  登录 30 次/10 分钟（记错密码试几次很正常，但撞库/枚举是实质约束）、
  上传 300 次/小时（一次最多选 100 张，远高于正常使用）。
- **单账号在飞行上限**：全局解码闸门只有 3 个槽位，一个账号发 3 个并发上传
  就能占满，其余所有用户的上传全部排队到超时 —— 这是一种**不需要打爆内存**的拒绝服务：
  内存曲线平稳，但别人都传不上去。这条保证 3 个槽位里至少有 1 个永远留给其他用户
  （上限 2 而非 1：客户端串行上传，正常单用户只占 1 个；给 2 是为了留重试/并发余量）。
- **上传错误码分家 #2**：单账号在飞上限最初复用了 1020（当日配额用尽），
  而客户端把 1020 判为不可重试。结果是"等前面几张传完就能成功"这种纯瞬时状态
  被当成了永久失败 —— 又一次"照片会消失"。已另开 1028，并把这份映射抽成
  客户端纯策略对象 `UploadRetryPolicy`（之前在 `PhotoUploader` 内部、
  永远测不到），写 4 个测试挡住"图省事复用现成的码"。

### 客户端

- **上传重试判定的回归测试**：这份映射与 `server/album_media.go` 的
  `codeUpload*` 常量是**跨端契约**，而两边不一致时的表现极具误导性：
  照片被永久标成失败、用户以为"照片消失了"（本轮就踩了一次）。
  已抽成纯策略对象 `UploadRetryPolicy` 并写测试，含"未知码默认可重试"的断言
  （旧版 App 遇到服务端新增码时的安全侧 —— 默认可重试只是多试一次，
  默认不可重试会让照片永久失败）。

### 测试

- **测试必须真能抓到 bug**：两条重要测试都按 AGENTS 2.8「先临时改坏、
  确认会红、再改回来」跑了一遍。GIF 帧炸弹测试只断言错误码的话，
  DecodeAll 与结构扫描的错误码一模一样、测试照样绿 —— 那等于没测。
  加上内存度量后把实现改回 DecodeAll 会分配 982 MB，立即红。
  另一条是单账号在飞上限的并发测试：单轮只有 1/4 概率抓到"先查后写"的击穿，
  一个 3/4 概率放过 bug 的测试等于没有 —— 它会以"CI 偶尔红一次"的形式存在，
  然后被当成抖动忽略掉。改成 60 轮累计判定后破坏实现跑 15 次全红。
- **修掉一个时间敏感测试**：`TestBatteryCurveWithNullColumns` 在本地时间
  00:00~00:10 之间必红（base 截到 00:05，减 10 分钟就跨到前一天 23:55，
  被当日查询排除 → 只剩 2 点）。这与被测逻辑无关，纯粹是测试自身的时刻依赖，
  会以"午夜 CI 红一次"的形式存在。已改为把基准锚到当日本地正午，
  与运行时刻无关。

### 安全、可靠性与部署

- **私密媒体真正收口**：相册原图、预览图与缩略图迁入私有媒体目录，统一经过
  `/media/<id>` 鉴权代理；回收站只允许已有缩略图，禁止通过预览或原图回退绕过
  隐私边界，并补上旧路径迁移与升级测试。
- **绑定与管理员并发安全**：绑定、生成邀请码、解绑均增加竞态保护；后台禁止并发
  删除、禁用或降级最后一个启用中的超级管理员；数据库查询故障不再被误报成“未绑定”。
- **上传与请求边界**：统一限制 multipart/JSON/文件大小，加入幂等键、IP 限流、
  HTTP/SMTP/WebSocket 超时和资源清理；清理 SQL 返回实际影响行数并记录错误。
- **配置一致性**：后台配置先完整校验、再事务提交，避免 map 遍历或中途故障造成
  半套配置；客户端开关现在真正控制相册、评论点赞与“这一天”的入口，服务端继续
  作为最终权限边界。

### 客户端与后台

- **相册分页稳定性**：防止重复触发分页、旧请求覆盖新列表，以及瞬时分页失败被
  永久当成“到底”；失败项现在可重试。
- **后台依赖安全**：升级 Axios 与 vue-i18n 受影响版本，生产依赖审计通过。
- **发布与部署对齐**：修正私有媒体卷、反代路径、非 root 容器目录权限、缓存响应头
  与初始凭据文档，确保镜像构建后的实际运行行为与代码一致。

## [1.0.2] - 2026-08-28

**内存泄露导致生产崩溃的修复集。** 起因是生产环境"跑一段时间就崩"，
定位到服务端内存态的三处结构性缺陷，并顺带修掉客户端两处会放大 OOM 的问题。
同时补上四处权限收敛 —— 其中一条等于把普通管理员账号变成了全站私密内容的钥匙。

**这一版建议尽快升级**：v1.0.1 的服务端内存只增不减，
且任何人无需登录就能加速这个过程（见第一条），最终结果是容器 OOM 重启。

### 服务端 · 内存与稳定性

- **★ 内存只增不减（本轮 OOM 的直接原因）**：进程内存态的五张表
  **只有"惰性过期"、没有任何清扫**。而"惰性"的前提是键过期后还会被再读一次，
  偏偏最主要的两类键**永远不会被读第二次**：
  - `login:fail:<账号名>` —— 键由**登录时提交的账号名**构成，
    而登录接口是公开的、账号名不限长度也不校验是否存在。
    枚举一轮不存在的账号 = 留下一轮永久驻留的条目，**无需登录即可触发**；
  - `media:cnt:<日期>:<uid>` / `media:bytes:<日期>:<uid>` —— 按**日期**分桶，
    过了当天谁都不会再读它，于是每天每个上传过照片的用户都留下两条永久垃圾。

  更糟的是读取计数器时**过期只返回 0、不删键**，
  连"被读到时顺手清掉"这条唯一的回收路径也是断的（真正会删的那个函数
  只在**登录成功**时调用，而垃圾全部来自失败的那些）。
  键长还不设上限，一个几 MB 的账号名字段就能换来一条几 MB 的常驻键。

  修法是三道独立的闸，任一道单独都足以兜住：定时清扫全部表（每分钟）、
  所有读写入口统一收敛键长（超长改用哈希，**不用截断** —— 截断会让两个不同的
  超长账号名撞成同一个键，等于能替他人清零限流）、容量上限 + 到顶时按最早过期淘汰。
  让"内存无界"在结构上不可能，而不是依赖清扫一定跑得过增长。
- **★ WebSocket 写操作没有超时，会永久占住协程**：对端不读数据时
  （客户端被冻结、进隧道、或恶意构造）写操作会**永久阻塞并一直持有写锁**。
  后果不是"某条消息发不出去"：待办提醒是**全站唯一**一条协程，
  它串行遍历到点待办逐个投递，卡在任意一个接收者身上就意味着
  **所有人的待办提醒从此彻底停止，且没有任何报错**；后台群发同理，
  管理员每点一次就多悬挂一条协程与一整份用户列表，永不回收。
  已加 10 秒写超时，超时即视为连接损坏、主动关闭并降级进离线补偿队列。
- **★ HTTP 服务器四个超时全部缺失**：这些字段的零值是**永不超时**，
  于是一条只发了半行请求头就不动的连接会永久占着一个协程与其读写缓冲，
  生产上表现为内存与协程数只涨不跌且**没有任何错误日志**。
  已补齐请求头 15 秒 / 读写各 5 分钟 / 空闲 90 秒。
  读写给到 5 分钟是为了覆盖弱网下传 20MB 大图 —— 设成常见的 30 秒会让
  弱网用户在上传时被服务端掐断，现象与"上传总是失败"一模一样。
  WebSocket 不受影响（升级后由长连接自己的 45 秒读超时接管）。
- **GIF 帧炸弹可单请求打爆进程**：像素上限校验拿到的是**首帧**尺寸，
  而随后的解码会把**每一帧**都解成独立位图。于是一个 20MB 以内、
  单帧仅 1000×1000（轻松过检）、但有数千帧的 GIF，内存占用按帧数线性增长。
  头像链路的 120 帧限制是在解码**之后**才检查的，拦不住；相册链路连那道检查都没有。
  改为先做一次**只读结构扫描**数帧（不解像素、内存与帧数无关，超限立即中止），
  再只解首帧。新增 8 个测试，含"提前中止而非读完再判"的断言。
- **图片解码没有并发上限**：单张最坏约 256MB，而解出的图要一直活到
  缩略图与预览图都写完。此前 N 个用户同时上传就是 N × 数百 MB。
  已加全局闸门（同时 3 个作业，20 秒取不到就回可重试的"服务器正忙"），
  相册与头像共用同一个闸门 —— 各自独立计数的话两条路加起来照样会超。
- **SMTP 发信没有超时**：建连与发信都在请求协程里同步跑，
  SMTP 主机被防火墙丢包（黑洞而非拒绝）时那条请求会永久悬挂，
  而发送冷却按邮箱分桶、换个邮箱即可再挂一条。已加 20 秒整体超时。

### 服务端 · 权限与隐私

- **★ 普通管理员可越权成为任意用户的"伴侣"**：绑定关系列表把邀请码原样下发，
  而挂起的邀请码就是"成为某人伴侣"的**凭据本身**。
  普通管理员挑一条挂起邀请抄走码，在 App 注册个账号调绑定接口即可绑定成功，
  从此该用户的相册、原图、待办、状态历史（含 WiFi 名与前台应用）**全部合法可读**
  —— 而相册与日记导出这些接口早就特意收敛到超管了，这条口子把那些收敛整个绕过。
  改为只下发"有没有挂起邀请"这个布尔值。**也不做部分脱敏**：
  邀请码只有 8 位，泄露任何一段都在成倍缩小爆破空间。
- **审计日志明文记录 SMTP 主机与账号**：脱敏规则只匹配三个关键词，
  于是 SMTP 主机、用户名、发件人落进兜底分支被明文写入 ——
  而审计日志页对普通管理员开放。系统设置之所以收敛到超管，
  理由正是"SMTP 主机与账号本身就是攻击面"，
  超管每改一次配置就等于把它抄送给所有普通管理员。
  判定改为**白名单**（只有站点名/LOGO 这类本就公开的项才记具体值）：
  白名单的默认行为是脱敏，将来新增的敏感键会自动落在安全那侧；
  黑名单的默认行为是明文，漏一个就泄露一个。
- **普通管理员可触发不可逆的照片删除**：回收站保留天数对普通管理员开放，
  改小它会让下一次定时清理**真删数据库记录 + 真删磁盘上的原图与缩略图**，
  用户以为"还在回收站里能恢复"的照片会在几小时内消失，且无任何撤销途径。
  同组的安全限流参数也一样可改 —— 那是往放松方向调爆破防护。
  两组现已收敛为超管专属（读仍开放，后台要显示当前值），非超管改动一律拒绝；
  相册配额这类真正无害的运行参数仍对普通管理员开放，没有一刀切。
- **禁用账号的登录响应会泄露口令是否正确**：后台登录先验密码、通过后才看封禁状态，
  于是"密码错"与"密码对但已被禁用"返回不同状态码 —— 拿一个已禁用账号爆破时，
  后者就等于"这个口令是对的"。而账号被禁用往往正是因为它已经不可信。
  两种情况已合并为完全一致的响应。
- **上传日志泄露真实磁盘路径**：预览图生成失败时把磁盘相对路径写进了日志，
  与"真实路径不出服务端"的既定底线冲突（容器日志常被随手查看）。改为只记尺寸与错误。

### 服务端 · 配置与数据正确性

- **「相册功能总开关」形同虚设**：拦截中间件写好了却**从未挂载到任何路由**。
  关掉开关后只有上传被挡，建相册、改名、挂照片、评论点赞照旧可写 ——
  管理员以为已经止损，实际只关掉了一半。现已挂载，且按方法放行读取
  （用户仍能查看和导出已有照片，不会以为照片丢了）。
  「评论与点赞」「这一天」两个开关此前同样只有客户端隐藏、服务端零校验
  （对旧版 App 与直接调接口的人完全无效），也已补上服务端拦截。
- **四项后台配置改了完全无效**：响铃冷却窗口与次数、安抚/冷静冷却、
  后台与 APP 的登录有效期，读的都是配置文件或代码常量而非后台设置。
  其中两个冷却值还会**下发给客户端**用于置灰按钮：管理员把响铃冷却改成 60 秒后，
  客户端按 60 秒解禁、服务端仍按 600 秒拒绝，**用户点了就报"过于频繁"**。
  一个配置项被两边用不同的值解释，比它压根不生效更难排查。四项均已接上后台配置，
  超频回执的文案也改为按当前配置生成（写死"10 分钟 3 次"在改过配置后就是错误信息）。
- **登录失败上限的语义整个错位**：APP 端登录误读了「后台登录失败上限」这一项，
  而真正的后台登录写死 5 次、完全不受配置影响。现已拆成两项独立配置。
  同时把这几项的上限从 100 收到 20 —— 它们都是**削弱**防护的方向，
  给不出需要 100 次失败的正当场景。
- **13 处数据库遍历缺少错误检查**：遍历中途出错时与"正常读完"在代码上无法区分，
  于是"少了几行的结果"被当成功返回。影响最直接的三处：
  群发通知的收件人列表被截断会让部分用户收不到通知，而后台仍显示"已发送 N 条"；
  待办定时扫描被截断会让部分提醒迟到一分钟；存储统计的合计值是循环累加出来的，
  少几行就是**直接显示错误的数字**，而页面上看不出任何异常。

### 客户端

- **★ 上传失败会引发连锁 OOM**：图片预处理里位图回收写在正常流程末尾，
  磁盘满、编码失败或首次 OOM 都会跳过它，每次留下一份约 12.6MB 的位图等回收。
  而上传失败会被标记为"可重试"，用户点「重试失败项」就在内存已紧张的情况下
  再走一遍、再漏一份 —— **每次重试都让下一次更容易失败**。
  用户侧的现象正是此前反馈过的"照片会消失"。已改为无论成败都保证回收，
  编码失败时也会删掉半截文件（留着会被大小校验判成"图片过大"，给出误导性提示）。
- **旋正与缩放的双份内存峰值**：位图变换在返回前源图与目标图同时驻留堆上，
  而旋正与缩放是两次独立调用，于是这个峰值要经历两遍（约 25MB）。
  竖着拿手机拍照对应的 EXIF 方向是**常态而非边缘情况**，
  一次最多选 100 张、逐张处理。已合并为一次矩阵变换，只有一次峰值；
  且解码期已缩到目标尺寸时完全不再分配。
- **壁纸常驻十几到几十 MB**：壁纸是裸的全尺寸解码（无边界探测、无降采样、
  无尺寸上限），而那个路径只是本地配置里的一个字符串、尺寸不受代码任何约束。
  解出的位图位于**界面最底层**、整个进程生命周期不释放，直接吃掉相册解码的余量
  —— 相册那边一旦 OOM，用户看到的是"照片上传失败"，根因却在壁纸功能里。
  已按屏幕像素预算降采样。判据刻意选"像素预算"而非"不许放大"：
  后者在图片长宽比与屏幕不匹配时**根本不会触发降采样**，横图配竖屏照样解 45.8MB。
  壁纸上面盖着遮罩，轻微软化几乎不可见，而省下的几十 MB 是相册能否成功解码的关键。
- **通知栏头像每 10 秒产生 1MB 垃圾**：那个叫"缓存"的对象其实没有缓存，
  每次重画卡片都重新解码一张新位图且不回收。前台档位每 10 秒采集一次状态，
  在常驻服务里持续制造回收压力。已按文件指纹复用，换头像时主动回收旧的那份。
- **开机广播的协程作用域挂在接收器实例上**，会让接收器实例被协程持有；
  且网络请求无超时，挂住时既不结束广播（系统按无响应处理）
  又一直持有整份待办列表。已改为进程级作用域 + 8 秒整体预算。

### 测试与验证

新增 20 个测试（服务端 14、客户端 6），全部针对**具体的旧行为**：
内存态清扫与键长收敛（含"改回旧实现必须变红"的实测验证）、
GIF 帧炸弹与提前中止、旋正缩放合并前后产物尺寸一致、壁纸解码预算。
另用浏览器实测后台 12 个页面（登录 → 首登改密 → 逐页巡检）零控制台错误、零失败请求，
并对四项权限收敛做了端到端越权验证（12 项全通过）。

## [1.0.1] - 2026-08-25

发版前全量代码审查的修复集。三端逐项审查后修掉 9 处问题，
其中 4 处是「会打死生产」或「用户什么都没做就崩」的级别。

**这一版建议尽快升级**：v1.0.0 的服务端有一条启动死锁，
生产库运行超过回收站保留期（默认 30 天）后，容器重启就会挂死在清理任务上、
整个服务不可用。详见下面第一条。

**服务端**

- **容器启动即全站瘫痪**（最严重，已实测复现）：`siteBaseURL()` 是惰性缓存，
  而它被 `scanPhoto` 在 4 处 `for rows.Next()` 循环里调用。`MaxOpenConns(1)` 下
  遍历 rows 期间那条唯一连接被占着，此刻冷缓存触发的查库要等它释放，
  而它要等遍历结束才释放 —— **自己等自己，永久死锁，那条连接再也不回池**。
  最确定的触发点是启动时同步跑的 `runRetentionCleanup` → 清理回收站 → 遍历 photo 行。
  新库撞不到（没有过期回收站数据），已有测试也撞不到（`st==nil` 时短路），
  所以这条路径只在「生产库跑过保留期」时炸。
  改法：`siteBaseURL` **绝不查库**，冷态返回空（客户端会补全）；
  加载交给显式的 `warmSiteBaseCache`（启动时 + 配置变更后各一次，都不在 rows 遍历中）。
  让死锁在结构上不可能，而不是靠"第一次之后就有缓存了"。
- **后台点「导出日记」会打死生产**：同一条死锁 —— 外层 rows 未关就调
  `diaryImageURLs` 内部再查询。改成先读完关 rows 再取图片地址。
- **回收站「还剩 N 天」永远不下发**：`for _, p := range list` 拿到的是值副本，
  写上去等于没写；而该字段是 `*int` + `omitempty`，写不进去就**从 JSON 里彻底消失**。
  改用下标遍历。
- **一次格式错误的请求能静默封禁用户**：`handleAdminSetUserStatus` 丢弃
  `ShouldBindJSON` 的返回值且 status 无白名单 —— 解析失败时 `req.Status` 是 0，
  而 `status != 1` 即视为禁用；传 `status: 7` 也能落库，之后没有界面能改回来。
  同款问题在版本状态接口。两处都补了 err 检查与取值白名单。
- **绑定关系可能被覆盖**：`BindPair` 里读 pair 成员的 `Scan` 错误被忽略，
  零值会让代码既跳过「不能和自己绑定」的校验，又走进 `userA == 0` 分支
  用 UPDATE **冲掉已存在的 user_a_id**。补了错误检查。
- 另 3 处 COUNT 的 `QueryRow().Scan()` 补上错误留痕（看板/网络日志/日记计数），
  出错时降级为 0 但会写日志 —— 否则「有数据却显示共 0 条」查不出原因。

**客户端**

- **状态历史与相册详情滚到底会崩**：服务端 OFFSET 分页 + 客户端拿「已加载条数」
  当 offset，列表持续增长时必然错位 —— 拉下一页前若有新数据写入，全体下移，
  offset 把上一页最后那条又返回一次，`items(key = ...)` 撞重复 key 直接抛异常。
  状态历史每 5 分钟落一条，傍晚超过一页后**滚到底必崩，而滚到底是自动触发的**；
  相册则是**对方**上传时触发（自己上传走整页重载，所以自测发现不了）。
  新增 `PagingMerge` 纯策略做去重 + 「整页全重复」时判到底，7 个单测。
- **头像裁剪拖动后裁歪**：`AvatarCropper.crop` 的 `viewSizePx` 默认取预览图长边
  （限死 1080），而 `offsetX/offsetY` 来自手势、单位是**容器像素**（一加 15 上 ≈1272）。
  两者差约 18%，于是**拖动位移被放大，拖得越多偏得越远**；不拖动时正好抵消，
  所以"居中裁"看起来完全正常。改成由画布回传实际容器边长并显式传入。
- **上传中离开页面会重复上传整批**：`onPickedConsumed()` 写在上传循环之后，
  用户中途返回列表页会取消协程 → 它永远执行不到 → `pickedUris` 仍非空 →
  再进任意相册就把整批重传一遍（100 张能直接撞掉每日配额）。移进 `finally`。
- 「这一天」页补掉 O(n²)（`items` + `indexOf` → `itemsIndexed`），
  与相册详情早先修过的是同一个坑。
- 「这一天」/回收站/选图器三处图片位补占位底色 —— 此前加载失败是完全透明的。

**CI**

- **`release.yml` 加 prerelease 开关**：原先 `prerelease: false` 是写死的、
  没有对应输入项，根本发不了预发布版。现在是 boolean 输入且**默认 true**。
- **发行版流水线补齐测试**：此前它从头到尾不跑任何测试，而 `build-android.yml`
  与 `build-server.yml` 都跑 —— 等于日常 CI 拦得住的坏提交，恰好在产出
  「用户真正安装的那个包」时放过去了。现在 gofmt/vet/go test 与安卓单测都在
  构建 APK 之前，任一失败就不产出 release。
- `AGENTS.md` 补 2.12~2.15（惰性缓存不能放在 rows 遍历路径上、OFFSET 分页要去重、
  值切片遍历改不了元素、后台状态接口要校验 err 与白名单）。

## [1.0.0] - 2026-08-22

首个正式发行版。0811 起共五轮开发，本节按模块汇总，不再按开发轮次分段。

### 你能用到的功能

- **实时状态共享**：绑定伴侣后互相可见电量与充电、亮屏/息屏（含息屏显示 AOD）、
  正在使用的应用、正在播放的音乐、网络与 WiFi。需双方在 App 内完成知情同意才开启，
  可随时关闭，关闭即停止采集。
- **伴侣状态历史**：按天查看对方的状态时间线与电量曲线。
- **共同相册**：上传照片（单次最多 100 张）、建分组、拖动管理、评论点赞、
  「这一天」回顾、回收站（可恢复、可彻底删除、显示还剩几天自动清理）。
- **待办与提醒**：给自己或对方添加待办，支持「仅一次 / 每天 / 每周指定几天」循环提醒，
  可开强提醒（走闹钟音量），也可随时暂停某条提醒。
- **远程互动**：一键发「求陪伴 / 求冷静 / 响铃提醒」，响铃 7 秒自动停且可双向撤回。
- **恋爱天数与资料**：纪念日、头像（圆形裁剪）、昵称、性别、简介。
- **同步自检页**：逐项检查保活相关权限，并写明「不开会怎样」。
- **应用内检查更新**。

### 客户端（Android）

- **全 App 统一 miuix 风格**：31 处 Material 图标换成 `MiuixIcons`，UI 层 `material3` 清零
  （仅配色链保留），响铃全屏页改 Compose + miuix（外层 try/catch 回退 XML——
  锁屏唤醒时序敏感，宁可丑不能哑），冷启动帧换 `DeviceDefault` 并与 miuix 背景对齐。
- **弹窗规范焊进组件**：`LxConfirmDialog` / `LxFormDialog` 把「取消灰底在左、确认蓝/红在右、
  危险操作 1 秒冷静期」做成组件行为，不再依赖每个调用点自觉。
- **按钮最小触达尺寸焊进组件**：`LxButton` 用 `defaultMinSize` 兜住 48dp。
- 自研 miuix 风格选图器：按相册分桶（相机 / 截屏 / 微信 / 下载）、分页加载、角标预览大图，
  顶栏常驻「系统相册」入口走系统 Photo Picker 兜底（无需任何读取权限）。
- 头像改用同一个选图器 + 圆形裁剪页（此前直接居中裁，竖构图人像的头会被切掉）。
- 相册三档图：thumb 384（网格）/ preview 1080（大图页先出）/ origin（放大后才拉），
  大图页预加载相邻两张。
- 自己上传的照片不走网络：`LocalPhotoIndex` 记住「照片 id → 本机原图 uri」，
  查看时直读本机、本机删了才回云端。磁盘缓存 128MB，设置页可清。
- 上传失败逐张给出具体原因并可重试失败项（此前一句"格式不支持或超过 20MB"
  把 OOM、解码失败、配额用尽全糊在一起）。
- 动态 WebP 不再被压成静图（`Bitmap.compress` 只写单帧，动画会整个丢失）。
- 状态采集重写：`Display.getState()` 作权威来源并单独识别 AOD；前台应用查询窗口
  24 小时→60 秒并逐级回退，认 PAUSED/STOPPED（回桌面不再显示上一个应用），息屏不查；
  采集移到 IO 线程（此前在主线程、前台档 10 秒一次，是 ANR 风险）；
  WS 断线改走 `POST /status` 兜底（此前直接丢弃，地铁里状态完全停更）；
  主页显示「更新于 N 分钟前」，过期置灰、失联不再把旧值当现状。
- 伴侣状态改 `StateFlow`。此前读普通 `@Volatile` 字段，Compose 不会重组——
  这才是「状态同步不实时」的真因，轮询再密也没用。
- 同步节奏分档：前台 10s / 后台 60s / 息屏 5min；WS 心跳 15s、服务端判死 45s，
  重连退避加 ±20% jitter 避免双端同时重连。
- 静默通知渠道 `status_quiet`（低优先级、无声、无振动、不亮灯）：伴侣息屏/亮屏、
  上线/下线只落通知栏。全部渠道收敛到 `NotificationChannels` 统一创建——
  `createNotificationChannel` 改不了已存在渠道的重要性与声音，谁先创建谁决定行为，
  此前 4 处各自创建等于把行为交给竞态。
- 引入 Coil 3 做图片加载，替代原先在主线程 `BitmapFactory` 解码的做法。

### 服务端

- **21 项运行参数可在后台调**（相册配额、数据保留天数、验证码与登录限流、token 有效期、
  邀请码、互动冷却），分 5 个分区、带范围收敛与一键恢复默认（默认值由服务端下发，
  永远与代码一致）。改完立即生效，无需重启容器。连同站点/SMTP/存储共 32 项可配。
- **真支持 HEIC / AVIF / BMP**：用 gen2brain 的纯 Go wasm 解码器（wazero，无需 CGO），
  不破坏「单容器 + 纯 Go」架构，也不必给 alpine 镜像装 libheif。
- 图片鉴权代理 `GET /media/:id`（及 `/thumb`、`/preview`）：真实磁盘路径不出服务端，
  只有该 pair 的成员能读；照片 URL 不进 `request_log`。
- 上传配额改原子占额（先记账后判断、超了回退）。此前"先查后写"两步，
  客户端改成并发上传后配额形同虚设。
- 三处「磁盘只涨不跌」全修：回收站按天清理并真删磁盘文件、状态历史加保留期、
  网络日志保留天数可配。每类清理都记录实际删除行数。
- 相册回收站补齐彻底删除与清空（真删文件），并给出「还剩 N 天自动删除」。
- 后台新增**相册管理**与**磁盘统计**页（各 pair 占用、回收站占用、真实磁盘占用），
  可清空指定情侣的回收站。后台查看缩略图只给 384、每次写审计、响应头带 `no-store`。
- 照片 `Cache-Control` 1 天→30 天 + `ETag`；GIF 缩略图改出 JPEG（PNG 大好几倍）。
- 原图按原字节保存不重编码（保画质与 EXIF）。
- 数据库启动自动建表（内嵌 `schema.sql`），任何环境零手动导入。
- 结构化日志（`log/slog`）+ 请求 ID；网络日志页保留天数可配。
- 头像/照片处理去 libvips 改纯 Go 解码。

### 运营后台

- 移动端适配：`art-table` 在 ≤768px 卡片化（复用同一份列配置与插槽，11 个页面零改动全受益）、
  搜索区窄屏纵向堆叠、`viewport-fit=cover` + 安全区。
  新增 `admin/scripts/mobile-audit.mjs`：Playwright 四档视口逐页断言无横向溢出、
  无控制台错误、无失败请求、非白屏。
- 「内容审核」相册照片 tab：元数据表格、关键词与 pair 筛选、真分页、软删二次确认。
- 通知定向投递可用（此前 `target` 字段是假的，选「指定用户」也永远全站广播）。
- 待办管理、用户管理、网络日志、审计日志、通知模板、管理员管理。

### 移除

- **「日记」功能整体下线**（客户端页面与模型、服务端 5 个 handler + 7 个 store 方法 +
  路由 + WS 消息 + 推送、`diary` / `diary_image` 两张表、后台页面与仪表盘统计卡、文档）。
  App 名称「林曦日记」保留，但没有写日记这个功能。
  下线前提供 `GET /api/admin/diaries/export` 导出 Markdown 留档；
  删表由 `POST /api/admin/diaries/purge?confirm=DROP` 显式触发，**刻意不在自动迁移里跑**
  ——否则新镜像一上线数据就没了，导出接口会变成废物。
- 死代码：`SAFE_MODE`、`DemoContent`、壁纸裁剪页（无任何入口可达）、
  「跳过（开发调试）」入口（含生产后门）。

### 修复 · 客户端

> 以下五条都追到了具体代码行，其中多条本机跑出复现。记下来是因为它们的表象与真因相距很远。

- **上传从来没成功过一张**：`ImagePrep` 读图片边界那步写成
  `openInputStream(uri)?.use { decodeStream(it, null, bounds) } ?: error("无法读取所选图片")`。
  `use{}` 返回的是 lambda 的值即 `decodeStream` 的返回值，而 `inJustDecodeBounds = true` 时
  **`decodeStream` 按设计永远返回 null**（只填 `outWidth`/`outHeight`，不产出 Bitmap）。
  于是 `?:` 恒成立，**除 GIF 与动态 WebP 外每一张图都在上传第一步抛异常**。
  判定已抽成可单测的 `ImagePrepPolicy.boundsFailure`，区分"流没打开"与"尺寸为 0"两种失败。
- **连续上传必 OOM**：`sampleSize` 只能做 2 的幂，4000×3000（最常见的手机直出）
  算出 `sample=1` → 全尺寸解码 **45.8MB**，而失败只静默计数，表现就是"照片会消失"。
  已加 `decodeDensityScale` 在解码期直接出目标尺寸，实测 **45.8MB → 12.0MB**。
- **进「伴侣状态历史」必崩**：`status_history` 三个可空列被 Go 侧用 `string` 接，
  而 `store.go` **忽略了 `rows.Scan` 的返回值** → 整行保持零值 →
  `Ts.UnixMilli()` = `-62135596800000`（每行同值）→ 客户端 `items(key = { it.ts })`
  撞重复 key → 崩溃。已改 `sql.NullString`，并给**全库 18 处 `rows.Scan` 补齐错误检查**。
- **有些图片扫不到**：`MediaStoreImages` 写死 `limit=2000` 且按 `DATE_TAKEN DESC` 排序。
  `DATE_TAKEN` 来自 EXIF、**只有相机直出才有**，截图与微信图普遍是 NULL 排在最后，
  图片总数一旦超 2000 就被整批截断。已改 `COALESCE(DATE_TAKEN, DATE_ADDED*1000)` 排序
  + 去上限 + 分页 200 + 覆盖全部存储卷 + 按目录分桶。
- **缩略图与相册封面是"透明"的**：服务端图片地址是条件绝对的——后台 `site.url` 未配置时
  返回相对路径 `/media/<id>/thumb`，而 `app_setting` 表无种子数据，默认就是相对路径。
  客户端此前零补全，Coil 拿到无 scheme 的字符串不走网络 fetcher、当本机文件找 → 失败；
  又因为图片位没有背景色，结果是**字面意义上的透明**。
  新增 `MediaUrlPolicy.absolutize` 并收口在 Coil mapper（网格/封面/大图/头像一处全受益），
  同时给图片位补占位底色。注意 `BASE_URL` 带 `/api/v1` 而图片挂根路径，必须先取 origin。
- **选头像即崩**：单选用了 `PickMultipleVisualMedia(1)`，而该 contract 的 init 是
  `require(maxItems > 1)`。contract 在 composition 期构造，所以一进页面就崩。改用 `PickVisualMedia`。
- **头像裁剪框中间是黑的**：`BlendMode.Clear` 会把目标像素的 alpha 一并清零，
  遮罩层没有离屏合成层时绘制直接落在父画布上，把图片连同蒙版一起擦掉、露出不透明窗口表面。
  已加 `CompositingStrategy.Offscreen`。
- 修「两个加载圈一起转」：`KernelScreen` 的 `loading` 与 `isRefreshing` 可同真
  （初次加载没结束就下拉），改为互斥，一处修好所有列表页。
- 修待办循环提醒只响一次、重启后闹钟不重建、缺精确闹钟权限导致必崩、
  `NetworkReceiver` 静态注册在 API 24+ 完全失效、下拉刷新指示器被顶栏遮挡。
- 修「邀请方绑定后进不去主界面」：邀请方进入等待态并轮询绑定状态，
  对方一绑定即自动进主界面（服务端同时推 `paired` 事件，双保险）。
- 错误提示不再直接显示「HTTP 400/403」，改显示服务端返回的中文原因。

### 修复 · 服务端

- **生产 502**：`fail()` / `afail()` 返回错误前不排空请求体，而 Go 只自动排空 ≤256KB，
  超过就关连接 → Nginx 侧变成 502，把精心写的中文错误整个吞掉。已统一 `drainRequestBody`。
- **老库升级起不来**（`no such column: deleted_at`）：`runMigrations` 一趟执行整个
  `schema.sql`，而补列在最后。老库里表已存在 → `CREATE TABLE IF NOT EXISTS` 是空操作 →
  新列不会出现 → 紧随其后引用该列的 `CREATE INDEX` 直接报错，**容器起不来**。
  已拆成「建表 → 补列 → 建索引」三段。
  **只用全新临时库做测试永远测不到这条升级路径**，故补了「先建旧表结构再跑迁移」的回归测试。
- 「伴侣状态历史」此前查的是**你自己**的记录，已改 `?who=me|partner`；
  日期查询的时区错位（写入用本地、查询按 UTC）已修。
- 删掉 6 个废弃 setting 键与死配置 `storage.upload_max_mb`（代码里从未引用，配了没用）。

### 安全修复（附「不修会怎样」）

> 半年后回看需要知道为什么改，故逐条记录后果。

- **头像上传在生产必然 500**：处理链依赖 libvips，而运行镜像是 alpine、根本没装。
- **手机相册主力格式必失败**：魔数白名单漏了 JPEG。手机照片九成是 JPG，
  等于上传功能对绝大多数图片直接不可用。
- **`request_log` 清理永久静默失败 → 磁盘被打满**：清理 SQL 用了 MySQL 语法
  （`NOW() - INTERVAL 7 DAY`），在 SQLite 上不报错也不删任何行。日志表只增不减，
  最终撑满磁盘导致服务不可写。
- **私密照片 URL 被写进网络日志**：netlog 的 skip 前缀只写了旧的 `/uploads`，
  而实际落盘前缀是 `/upload`。任何能看后台「网络日志」页的管理员都能直接点开情侣私密相册。
- **普通 admin 事实上等于超管**：此前只有两个路由挂了 `requireSuper`，其余全裸奔——
  普通 admin 能读设置里的存储密钥、向全站群发通知、删任意数据、封禁用户、
  解绑他人情侣关系、上传 300MB 文件落盘。敏感路由已全部收敛到超管。
- **token 泄露后无法止损**：新增 `token_ver` 撤销机制（改密/重置/禁用/角色变更即令旧 token
  全失效），后台 token 有效期 720h → 2h；`AdminAuth` 改实时读库，不再信 token 里的
  `role`/`status`——否则把管理员降权或封禁后，他手上的旧 token 在过期前依然是超管。
- **邀请码可被暴力枚举**：6 位纯数字（100 万空间）升到 8 位混合字符
  （约 32^8 ≈ 1.1 万亿）+ 每账号 10 分钟 5 次上限。
- **初始超管口令写进日志**：改为写入 `0600` 权限文件。日志会被采集、轮转、
  随诊断包外发，口令跟着一起走。
- **WS 可被单帧打爆内存**：新增单帧 64KB 上限 + 上行限频；离线队列加上限与 TTL。
- **来源 IP 可伪造**：未设 `SetTrustedProxies` 时 Gin 信任所有代理，
  任何人都能用 `X-Forwarded-For` 污染审计日志并绕过一切按 IP 的限流。
- **debug 模式信息泄露**：生产默认切 gin release 模式。
- 其余：邮箱验证码尝试上限、全局安全响应头、跨域收紧、管理员首登强制改密、
  上传扩展名白名单、`/uploads` 禁嗅探、`JWT_SECRET` 缺省即拒绝启动、优雅关闭。
- 密钥改由环境变量 `JWT_SECRET` / `APP_KEY` 注入，不再写入提交的配置文件。

### CI 与发布

- **安卓固定签名**（PKCS#12，指纹恒定），详见 [docs/SIGNING.md](docs/SIGNING.md)。
  此前每次构建签名都不同，用户装新版会撞「签名不一致」而必须先卸载、连带丢本地数据。
- **修复 `release.yml` 完全没传 `VERSION_CODE`**：`build.gradle.kts` 因此回退默认值 1，
  每个发行版的 `versionCode` 都是 1。安卓要求它递增，用户**根本装不上更新**。
  现由 tag 推导：`v1.2.3` → `versionCode = 1*10000 + 2*100 + 3 = 10203`
  （单调递增且与版本名一一对应，minor/patch 超过 99 直接报错）。
- 镜像冒烟测试：构建后起容器 curl `/healthz`，60s 内不通过即失败——
  挡住「镜像推上去了但启动即崩」。
- APK 签名指纹打进 Job Summary + 发行版指纹断言（指纹变了就让流水线红，
  而不是等用户装不上才发现）。
- R8 `mapping.txt` 作为产物上传（否则线上崩溃栈全是混淆名，无法还原）。
- **Go 1.22 → 1.25**：HEIC/AVIF 解码器（底层 wazero）声明 `go >= 1.25`，
  `go mod tidy` 会把 `go.mod` 提上去，而 Dockerfile 与 CI 若还是 1.22 则镜像构建直接失败。
  本机恰好是新版所以完全没复现出来——引入新依赖后要检查它声明的最低 Go 版本。
- 服务端镜像除推 GHCR（私有）外，额外导出 `.tar.gz` 作为产物，国内可直接 `docker load`。
- 新增 `gofmt` 检查。

### 规范与文档

- 新增仓库根 [AGENTS.md](AGENTS.md)：弹窗按钮语义、miuix 组件与图标约束、
  Go 与 SQLite 易错点（`rows.Scan`、提前返回要排空 body、迁移顺序、清理 SQL 方言）、
  Kotlin 的 `?.use{}` 返回值陷阱、图片地址补全、可点区域下限、提交前三端自检、隐私底线。
  配一行 `CLAUDE.md` 引用。
- 新增 [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)（工具链与本地验证流程，
  含「本机代理会造成假 502」的排查警告）、[docs/SELFTEST_0821.md](docs/SELFTEST_0821.md)
  （真机自测清单，含「我已验证 / 我无法验证」的分界）。

[Unreleased]: https://github.com/Lxii-Build/LxDay/compare/v1.0.9...HEAD
[1.0.9]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.9
[1.0.8]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.8
[1.0.7]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.7
[1.0.6]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.6
[1.0.5]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.5
[1.0.4]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.4
[1.0.3]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.3
[1.0.2]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.2
[1.0.1]: https://github.com/Lxii-Build/LxDay/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.0
