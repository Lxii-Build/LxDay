# 更新日志 · Changelog

本项目所有值得记录的版本变更都记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（`vMAJOR.MINOR.PATCH`）。

> 约定：发行版由「发行版工作流」触发，首次发行同时提供《应用介绍》（见 `docs/APP_INTRO.md`），此后每个版本在此追加一条更新日志。

## [Unreleased]

### 0822（二）· 选头像即崩 + 缩略图"透明"

- **换头像必崩**：系统相册兜底入口在单选时用了 `PickMultipleVisualMedia(1)`，
  而该 contract 的 init 是 `require(maxItems > 1)` —— 传 1 当场抛
  `IllegalArgumentException: Max items must be higher than 1`。contract 在 **composition 期**
  构造，所以头像选图页**一进就崩**（崩溃栈里全是 Compose 渲染帧、没有点击事件，正好印证）。
  单选改用 `PickVisualMedia`。已补测试真实构造该 contract 验证（JVM 单测里能构造，
  传 1 复现出与线上完全一致的异常）。
- **缩略图与相册封面是透明的**：服务端图片地址是"条件绝对"的 —— 后台 `site.url` 未配置时
  返回**相对路径** `/media/<id>/thumb`，而 `app_setting` 表无种子数据，**默认就是相对路径**。
  客户端此前没有任何补全，Coil 拿到无 scheme 的字符串不走网络 fetcher、
  当本机文件找 → 失败 → 渲染空白。
  新增 `MediaUrlPolicy.absolutize` 并**收口在 Coil mapper 里**（相册网格/封面/大图/头像
  一处修全部受益）。注意 `BASE_URL` 带 `/api/v1` 而图片挂根路径，必须先取 origin 再拼。
  8 个单测，**两种错法（不补全、拼错成带 api 前缀）都验证过会红**。
- **图片位补占位底色**：相册网格与封面此前无背景色，**加载失败就是字面意义上的透明**——
  这让上面那个 bug 完全隐形。现在失败是可见的灰格子。
- 「点进大图还能看到」曾误导排查：大图页优先读 `LocalPhotoIndex` 记的**本机原图 uri**
  （管理员是上传者本人），压根没请求服务端。**"大图正常"不能证明网络链路是好的**，
  换伴侣账号看同一张会是空白。头像"正常"同理是假象：`NetworkAvatar` 在 url 为空时
  画的是昵称首字母圆圈，看起来就像个正常头像。
- 相册分类封面：服务端逻辑本就完整（显式 `cover_photo_id` → 回退最新一张），
  客户端渲染也在。此前看不到是同一个相对路径 bug，且上传修好前压根没有照片。
- `AGENTS.md` 补 2.10（图片地址条件绝对、补全要收口、BASE_URL 带 api 路径的坑）
  与 2.11（图片位必须有占位底色）。

### 0822 · 找到「一张都传不上去」的真凶

- **上传第一步就必抛异常**（这是"上传显示无法读取图片"与 Q57「服务器没成功上传过一张」的
  真正第一道墙，此前我把它归给了 502——502 确实存在，但它只是第二道）：
  `ImagePrep` 读图片边界那步写成
  `openInputStream(uri)?.use { decodeStream(it, null, bounds) } ?: error("无法读取所选图片")`。
  `use{}` 返回的是 lambda 的值，也就是 `decodeStream` 的返回值；而 `inJustDecodeBounds = true`
  时 **`decodeStream` 按设计永远返回 null**（它只填 `outWidth`/`outHeight`，不产出 Bitmap）。
  于是 `?:` 恒成立，**除 GIF 与动态 WebP（走 copyAsIs）之外的每一张图都在第一步抛异常**。
  这个坑从 `a0a699f` 引入 `ImagePrep` 起就在。
  已把判定抽成可单测的 `ImagePrepPolicy.boundsFailure`（区分"流没打开"与"尺寸为 0"两种失败，
  给不同文案），并补 4 个回归测试——**已验证把实现改回旧语义时这些测试确实会红**。
- **顶栏按钮太窄**：`LxButton` 只设了 `padding(vertical = 13.dp)`，横向零留白，
  于是顶栏里不带 `fillMaxWidth` 的调用点（「上传」「取消」「系统相册」）被压成文字本身的宽度。
  已加 `horizontalPadding` 参数，并用 `defaultMinSize` 把 48dp 最小触达尺寸**焊进组件**——
  靠每个调用点自己记着传参迟早会漏。
- 选图器行 URI 统一回规范形式（`content://media/external/...`）。0821 改成按卷遍历后
  行 uri 变成了 `external_primary` 形态，而 uri 是被持久化的（`LocalPhotoIndex` 存
  「photoId → 本机原图 uri」），形态一换老索引就全失配、"读本机原图"的优化静默失效。
- `AGENTS.md` 补 2.8（`?.use{}` 的返回值陷阱 + 判定逻辑要抽纯策略再单测 + 补测试后必须
  验证它在旧实现下会红）与 2.9（可点区域下限焊在组件里）。

### 0821 · 三条实证根因 + 相册/状态/后台大修

**先说三条根因（都追到了代码行，其中两条本机跑出复现）**

- **进「伴侣状态历史」必崩**：`status_history.foreground_pkg/foreground_name/ssid` 三列可为 NULL
  （未授「使用情况访问」或息屏无前台时必为 NULL），而 Go 侧用 `string` 接、
  且 `store.go` **忽略了 `rows.Scan` 的返回值** → 整行保持零值 →
  `Ts.UnixMilli()` = `-62135596800000`（每行同值）→ 客户端 `items(key = { it.ts })`
  撞重复 key → `IllegalArgumentException`。0820 加的那个 `key` 只是点燃了早就埋好的雷。
  已改 `sql.NullString` + **全库 18 处 `rows.Scan` 补齐错误检查**。
- **「有些图片消失、扫不到」**：`MediaStoreImages` 写死 `limit=2000` 且按 `DATE_TAKEN DESC`
  排序。`DATE_TAKEN` 来自 EXIF，**只有相机直出才有**——截图、微信保存、下载的图普遍是 NULL，
  在 DESC 排序里排最后，图片总数一旦超 2000 就被整批截断。
  已改 `COALESCE(DATE_TAKEN, DATE_ADDED*1000)` 排序 + 去上限 + 分页 200 + 覆盖全部存储卷。
- **上传大图必失败 / 生产 502**：`fail()`/`afail()` 返回错误前不排空请求体，而 Go 只自动排空
  ≤256KB，超过就关连接 → Nginx 侧变成 **502 Bad Gateway**，把中文错误整个吞掉。
  实测生产 body ≥2MB 且被中间件拒绝时必 502。已统一 `drainRequestBody`。
  另外客户端 `ImagePrepPolicy.sampleSize` 对 4000×3000 算出 `sample=1` →
  **全尺寸解码 45.8MB**，连续上传必 OOM 而失败只静默计数。
  已加 `decodeDensityScale`（解码期直接出目标尺寸），实测 **45.8MB → 12.0MB**。

### 客户端（Android）· 0821
- **相册可管了**：相册卡右侧「⋯」显式管理入口（此前只能长按且零提示，管理员因此以为"分组删不掉"）；
  网格**长按进多选** → 批量删除 / 移动到其它相册；大图页删除**加二次确认**（此前点一下就删）；
  「未归类」可改名；绑定时自动建「我们/日常/旅行」默认分组。
- **回收站补齐**：彻底删除、清空回收站（**真删磁盘文件**）、每张显示「还剩 N 天自动删除」（≤3 天转红）。
- **三档缩略图**：thumb 384（网格）/ preview 1080（大图页先出）/ origin。此前从缩略图直接跳 2048 原图，
  弱网点开要白屏等 3~5 秒。大图页另加相邻预加载。
- **自己传的照片不再走网络**：`LocalPhotoIndex` 记住「照片 id → 本机原图 uri」，
  查看时直接读本机、删了才回云端；磁盘缓存 256MB→128MB，设置页可清。
- 选图器学 QQ：按相册分桶（相机/截屏/微信/下载）、分页加载、角标预览、单次上限 20→100。
- 头像改用与相册同一个选图器 + **miuix 圆形裁剪页**（此前直接居中裁，竖构图人像的头会被切掉）。
- 动态 WebP 不再被压成静图（此前 `Bitmap.compress` 只写单帧，动画丢失）。
- 上传失败**逐张给出具体原因**并可重试失败项（此前一句"格式不支持或超过 20MB"把 OOM、
  解码失败、配额用尽全糊在一起）。
- **状态采集六处修复**：`Display.getState()` 作权威来源并区分 AOD（一加 15 默认开着）；
  前台应用查询窗口 24 小时→60 秒 + 逐级回退，认 PAUSED/STOPPED（回桌面不再显示上一个应用），
  息屏不查；采集移到 IO 线程（此前在主线程，前台档位每 10 秒一次，是 ANR 风险）；
  WS 断线改走 `POST /status` 兜底（此前直接丢弃，地铁里状态完全停更）；
  主页显示「更新于 N 分钟前」，过期置灰、失联不再把旧值当现状。
- 新增**同步自检页**：逐项体检保活权限并写明"不开会怎样"（此前是一堆没有状态反馈的跳转按钮）。
- **全 App 统一 miuix**：31 处 Material 图标 → `MiuixIcons`；UI 层 `material3` 清零；
  响铃全屏页改 Compose+miuix（外套 try/catch 回退 XML——锁屏唤醒时序敏感，宁可丑不能哑）；
  冷启动帧换 `DeviceDefault` + 与 miuix 背景对齐的底色。
- **修「两个加载圈一起转」**：`KernelScreen` 的 `loading` 与 `isRefreshing` 可同真
  （初次加载还没结束就下拉），改为互斥，一处修好所有列表页。
- **弹窗规范落地**：新增 `LxConfirmDialog`/`LxFormDialog`，把"取消灰底在左、确认蓝/红在右、
  危险操作 1 秒冷静期"焊死在组件里，不再依赖每个调用点自觉。
- 删除死代码：`SAFE_MODE`、`DemoContent`。

### 服务端 · 0821
- **后台可配置项从 11 个扩到 33 个**：新增 22 项运行参数（相册配额、数据保留天数、
  验证码/登录限流、token 有效期、邀请码、互动冷却），分 5 个分区、带范围收敛与
  **一键恢复默认**（默认值由服务端下发，永远与代码一致）。改完**立即生效，无需重启容器**。
- **三处「磁盘只涨不跌」全修**：回收站按天清理并真删文件、状态历史加保留期、
  网络日志保留天数可配。每类清理都记录实际删除行数——0820 那次 netlog 用了 MySQL 语法，
  在 SQLite 上永久静默失败而无人察觉。
- **真支持 HEIC/AVIF/BMP**：用 gen2brain 的**纯 Go wasm** 解码器（wazero，无需 CGO），
  不破坏「单容器 + 纯 Go」架构，也不给 alpine 镜像装 libheif。
- 上传配额改**原子占额**（先记账后判断、超了回退）：此前"先查后写"两步，
  客户端改成并发 3 路上传后配额形同虚设。
- 「伴侣状态历史」此前查的是**你自己**的记录（`currentUID`），已改 `?who=me|partner`；
  日期查询的时区错位（写入用本地、查询按 UTC）已修。
- 照片 `Cache-Control` 1 天→30 天 + `ETag`；GIF 缩略图改出 JPEG（PNG 大好几倍）。
- 后台新增**相册管理**与**磁盘统计**页（各 pair 占用、回收站占用、真实磁盘占用），
  可清空指定情侣的回收站。后台查看缩略图**只给 384、每次写审计、响应头 no-store**。
- 删掉 6 个废弃 setting 键与死配置 `storage.upload_max_mb`（代码里从来没引用过，配了没用）。

### 移除
- **「日记」功能整体下线**（客户端页面/模型/接口、服务端 5 个 handler + 7 个 store 方法 +
  路由 + WS 消息 + 推送、`diary`/`diary_image` 两张表、后台页面与仪表盘统计卡、文档）。
  0811 那轮只删了客户端入口、留下服务端孤儿接口，结果 0820 又接回来了——这次彻底断根。
  下线前提供 `GET /api/admin/diaries/export` 导出 Markdown 留档；
  删表由 `POST /api/admin/diaries/purge?confirm=DROP` 显式触发，**不在自动迁移里跑**
  （否则新镜像一上线数据就没了，导出接口会变成废物）。

### 规范与文档
- 新增仓库根 `AGENTS.md`（弹窗按钮语义、miuix 组件与图标约束、Go/SQLite 易错点、
  提交前自检、隐私底线）+ 一行 `CLAUDE.md` 引用；新增 `docs/DEVELOPMENT.md`（工具链与验证流程）。
- 后台移动端适配：`art-table` 在 ≤768px 卡片化（复用同一份列配置与插槽，11 个页面零改动全受益）、
  搜索区窄屏纵向堆叠、`viewport-fit=cover` + 安全区。
  新增 `admin/scripts/mobile-audit.mjs`：Playwright 四档视口（一加 15 / 360 / iPhone / 平板）
  逐页断言无横向溢出、无控制台错误、无失败请求、非白屏。


### 客户端（Android）· 0820
- **新增相册**：相册列表、详情网格、大图查看（Pager + 双指缩放）、「这一天」、评论、点赞、软删除。服务端 4 张表 / 21 个接口，接口文档见 [docs/ALBUM.md](docs/ALBUM.md)。
  - 回收站的服务端接口（`GET /photos/recycled`、`POST /photos/:id/restore`）已就绪，**客户端本轮尚无入口**：删除会进回收站，但 App 内还看不到、也恢复不了。下一轮补 UI。
- 新增自研 miuix 风格图片选择器（`PhotoPickerScreen`），替代原先的 SAF 文件浏览器；顶栏常驻「系统相册」入口走系统 Photo Picker 兜底。头像选择改用系统 Photo Picker。
- 引入 **Coil 3**（`coil-compose` + `coil-network-okhttp`）做图片加载，带内存 + 磁盘缓存，替代原先在主线程 `BitmapFactory` 解码的做法。
- 上传前预处理（`ImagePrep` / `ImagePrepPolicy`）：降采样解码 → EXIF 旋正 → 长边压到 2048 → HEIC/AVIF 转 JPEG。转码不是画质取舍而是必须：服务端解码链是纯 Go，不支持 HEIF。
- **日记功能入口恢复**：服务端接口与后台审核页此前都在，客户端却没有入口，功能等于不存在。
- 伴侣状态改为 `StateFlow` 响应式。此前读的是普通 `@Volatile` 字段，Compose 不会重组——这才是「状态同步不实时」的真因，轮询再密也没用。
- 同步节奏分档（`SyncIntervalPolicy`）：前台 10s / 后台 60s / 息屏 5min；WS 心跳 15s、服务端判死 45s（≈3 个心跳周期）、重连退避加 ±20% jitter 避免双端同时重连。
- 新增静默通知渠道 `status_quiet`（`IMPORTANCE_LOW` + 无声 + 无振动 + 不亮灯）：伴侣息屏/亮屏、上线/下线只落通知栏，不弹横幅不响铃。全部渠道收敛到 `NotificationChannels` 统一创建——`createNotificationChannel` 对已存在的渠道改不了 importance 与声音，谁先创建谁决定行为，此前 4 处各自创建等于把行为交给竞态。
- 响铃 7 秒自动停止（`RING_DURATION_MS`）+ 通知栏「停止响铃」按钮 + 双向撤回（`ring_cancel` / `ring_stopped`）；停止时还原音量与勿扰、取消振动。
- 互动按钮 7 秒态 + 双向冷却；加载动画统一。
- 修复：待办循环提醒只响一次、重启后闹钟不重建、缺少精确闹钟权限导致必崩、`NetworkReceiver` 静态注册在 API 24+ 完全失效、下拉刷新指示器被顶栏遮挡。
- 删除死代码：壁纸裁剪页（无任何入口可达）。

### 客户端（Android）· 0811–0813
- 修复「邀请方绑定后进不去主界面」：邀请方生成邀请码后进入等待态，每 3 秒轮询绑定状态，对方一绑定即自动进入主界面（服务端同时推送 `paired` 事件，双保险）。
- 新增前台定期同步：每 30 秒拉取一次伴侣资料/绑定态/纪念日，与实时推送互补。
- 错误提示友好化：不再直接显示「HTTP 400/403」等状态码，改为显示服务端返回的中文原因；网络异常给出「网络连接失败/超时」等可读文案。
- 「我的」页个人资料去重：移除重复的「我的头像 / 我的昵称 / 纪念日」内联项，统一由「编辑资料」页维护。
- 日志更规范：日志行加入源码位置（类.方法:行号），Release 关闭 DEBUG 落盘；崩溃日志补充线程名并脱敏，移除调试标记文件。
- 移除「跳过（开发调试）」入口（含生产后门）。
- 登录 / 注册页美化：品牌头区、统一圆角与克制阴影、字段前置图标与内联校验提示、状态栏/键盘适配；重做「发送验证码」按钮（固定宽度不跳动）。
- 待办本地闹钟仅在「被提醒者=本人」时于本机调度，避免提醒响错设备（跨设备提醒以服务端扫描推送为准）。

### 服务端（Go）· 0820
- 新增相册模块：`album` / `photo` / `photo_comment` / `photo_like` 四张表 + 上传、鉴权代理、点赞评论、「这一天」、回收站全套接口（见 [docs/ALBUM.md](docs/ALBUM.md)）。
- 新增图片鉴权代理 `GET /media/:id`（及 `/thumb`）：真实磁盘路径不出服务端，只有该 pair 的成员能读；照片 URL 不进 `request_log`。
- 头像/照片处理**去 libvips，改纯 Go 解码**（`image/jpeg|png|gif` + `golang.org/x/image/webp`）。
- 魔数白名单补上 JPEG。
- 上传配额：每人每天 200 张 / 500MB，计数落进程内存（按日期分桶，TTL 25h），落盘成功后才记账。
- 原图按原字节保存不重编码（保画质与 EXIF），另出长边 512 等比缩略图。
- 后台新增接口：SMTP 测试发信、管理员角色/状态/重置密码/删除、通知模板删除、审计日志筛选、相册照片审核、通知定向投递。

### 运营后台（Web）· 0820
- 「内容审核」新增**相册照片** tab：元数据表格（尺寸/大小/MIME/拍摄与上传时间/描述/状态）、关键词与 pair 筛选、真分页、软删二次确认。**刻意不显示缩略图**——服务端不返回图片 URL，管理员也没有用户 token，加缩略图等于凭空造一条隐私泄露面。该 tab 仅超管可见（服务端接口挂 `requireSuper`）。
- 通知定向投递可用：此前 `target` 字段是假的，选了「指定用户」也永远全站广播。
- 时间列统一走 `utils/format/datetime.ts`；新增 `utils/format/filesize.ts` 格式化字节数。

### 安全修复 · 0820（附「不修会怎样」）
> 半年后回看需要知道为什么改，故逐条记录后果。

- **头像上传在生产必然 500**：处理链依赖 libvips，而运行镜像是 alpine、根本没装 libvips。改纯 Go 解码后才真正可用。
- **手机相册主力格式必失败**：魔数白名单漏了 JPEG。手机照片九成是 JPG，等于上传功能对绝大多数图片直接不可用。
- **`request_log` 清理永久静默失败 → 磁盘被打满**：清理 SQL 用的是 MySQL 语法，在 SQLite 上不报错也不删任何行。日志表只增不减，最终撑满磁盘导致服务不可写。
- **私密照片 URL 被写进网络日志**：netlog 的 skip 前缀只写了旧的 `/uploads`，而实际落盘前缀是 `/upload`。任何能看后台「网络日志」页的管理员都能从日志里直接点开情侣私密相册。
- **普通 admin 事实上等于超管**：此前只有 `POST /admins` 与 `PUT /settings` 挂了 `requireSuper`，其余全裸奔——普通 admin 能读 `GET /settings` 里的存储密钥、能向全站群发通知、能删任意日记待办、能封禁用户、能解绑他人情侣关系、能上传 300MB 文件落盘。敏感路由已全部收敛到超管。
- **token 泄露后无法止损**：新增 `token_ver` 撤销机制（改密/重置/禁用/角色变更即令旧 token 全失效），后台 token 有效期 720h → **2h**；`AdminAuth` 改为实时读库，不再信 token 里的 `role`/`status`——否则把管理员降权或封禁后，他手上的旧 token 在过期前依然是超管。
- **邀请码可被暴力枚举**：6 位纯数字（100 万空间）升到 8 位混合字符（约 32^8 ≈ 1.1 万亿）+ 每账号 10 分钟 5 次上限。不修则可枚举他人邀请码，把自己绑进别人的关系里。
- **初始超管口令写进日志**：改为写入 `0600` 权限文件。日志会被采集、轮转、随诊断包外发，口令跟着一起走。
- **WS 可被单帧打爆内存**：新增单帧 64KB 上限（`SetReadLimit`）+ 上行限频；离线队列加上限与 TTL，否则长期离线的一方会让队列无限增长。
- **来源 IP 可伪造**：未设 `SetTrustedProxies` 时 Gin 信任所有代理，任何人都能用 `X-Forwarded-For` 污染审计日志并绕过一切按 IP 的限流。
- **debug 模式信息泄露**：生产默认切 gin release 模式（debug 会打印全部路由表与详细报错，且拖慢每次请求）。
- 其余：邮箱验证码尝试上限、全局安全响应头。

### 服务端 / 运营后台 · 0811–0813
- 数据库免手动导入：服务端启动时自动建表（内嵌 `schema.sql`），任何环境（Compose / 宝塔自带 MySQL / 复用旧卷）零手动导入。
- 容器内外端口统一为 `7740`（宝塔容器列表显示 7740 → 7740）。
- 新增「网络日志」：请求日志中间件（方法/路径/状态码/耗时/IP/UA/请求ID）+ 后台网络日志页，保留 7 天。
- 结构化日志：服务端引入 `log/slog`，注入请求 ID。
- 安全加固：待办/日记越权校验（按 pair 归属）、WebSocket 每连接写锁与空闲保活修复、跨域收紧、登录失败限流、管理员首登强制改密、超级管理员权限校验、通用上传扩展名白名单、`/uploads` 禁嗅探、JWT 密钥缺省即拒绝启动、优雅关闭、群发通知异步化。
- 密钥改由环境变量 `JWT_SECRET` / `APP_KEY` 注入（`.env`），不再写入提交的配置文件。
- 后台清理：移除模板残留页面（注册/找回密码/内嵌演示）与死数据/未用组件；顶栏精简；外链指向本项目；存储设置仅保留本地；管理员头像换为系统 LOGO 且站点 LOGO 设置生效。

### CI 与发布 · 0820
- **安卓固定签名**（PKCS#12，指纹恒定），详见 [docs/SIGNING.md](docs/SIGNING.md)。此前每次构建签名不同，用户装新版会撞「签名不一致」而必须先卸载（连带丢本地数据）。
- **修复 `release.yml` 完全没传 `VERSION_CODE`**：`build.gradle.kts` 因此回退默认值 1，每个发行版的 `versionCode` 都是 1。安卓要求 `versionCode` 递增，用户**根本装不上更新**。现由 tag 推导：`v1.2.3` → `versionName=1.2.3`、`versionCode=1*10000+2*100+3=10203`（单调递增且与版本名一一对应，minor/patch 超过 99 直接报错）。
- 新增镜像冒烟测试：构建后起容器 curl `/healthz`，60s 内不通过即失败——挡住「镜像推上去了但启动即崩」。
- 新增 APK 签名指纹打印到 Job Summary + 发行版指纹断言（指纹变了就让流水线红，而不是等用户装不上才发现）。
- 新增 R8 `mapping.txt` 产物（否则线上崩溃栈全是混淆名，无法还原）。
- 新增 `gofmt` 检查。

### 构建 / 部署 · 0811–0813
- 服务端镜像工作流在推送 GHCR（私有）之外，额外导出镜像 `.tar.gz` 作为产物 / Release 附件，国内可直接 `docker load` 导入。
- 安卓构建 / 发行版工作流在未显式传入时回退使用仓库 Secret `APP_KEY`。
- `container_name: LxDay`。

## [1.0.0] - 2026-08-11
首个公开发行版。

### 客户端（Android）
- 新增 登录 / 注册体系（注册为登录子页，返回键回登录不再退出到桌面）。
- 知情同意改为页内 Dialog：绑定或调试跳过后、凡已绑定且未同意进入主页即强制弹出，同意/拒绝后留在主页。
- 待办页重做为仿 KernelSU 模块卡：展示 事件 / 提出者 / 被提醒者 / 详情（可展开），保留提醒开关、新增删除键；添加待办可选择被提醒者（自己 / 对方）。
- 待办搜索修复：去首尾空格 + 防抖 + 支持按 事件/详情/提出者/被提醒者 匹配。
- 待办与伴侣状态历史支持「下拉刷新」（下拉/松开/正在刷新/刷新成功四态），搜索框固定、仅列表滚动；FAB 改为随滚动位移隐藏/显示。
- 「我的」页日志区精简为仅「发送日志」，发送日志弹窗（保存 / 发送 + 取消）与「关于」合并为相连卡片；关于页改用应用自身图标。
- 支持构建期注入 服务端地址 / 通讯密钥 / 版本号，请求附带通讯密钥头。

### 服务端 / 运营后台
- 待办新增 `remind_enabled` 字段（提醒开关）；被提醒者可为情侣任一方。
- 服务端内置托管运营后台静态资源与上传文件，去除 nginx 依赖（容器编排简化为 应用 + 数据库）。
- 新增通讯密钥中间件（`X-App-Key`）。
- 运营后台新增待办管理（提出者/被提醒者/提醒信息），用户管理展示资料字段。

### 构建 / 部署
- 重构为三条工作流：构建服务端镜像（推送 ghcr.io）、构建安卓客户端、发行版。
- 完善 README 与部署文档（Docker Compose 为主 / 单容器 / 前后端分离，含反向代理 TLS 指引）。

[Unreleased]: https://github.com/Lxii-Build/LxDay/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/Lxii-Build/LxDay/releases/tag/v1.0.0
