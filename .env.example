# 林曦日记 · Docker Compose 环境变量示例
# 复制为 .env（勿提交仓库），填入真实值后 `docker compose up -d`。
#
# JWT_SECRET：登录令牌签名密钥。服务端运行时必需，用一个长随机串。
#   生成示例：openssl rand -hex 32
JWT_SECRET=please-generate-a-long-random-secret
#
# APP_KEY：通讯密钥（X-App-Key），与安卓客户端构建时注入的必须一致；留空则禁用校验。
APP_KEY=
