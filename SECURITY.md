# 安全政策

## 报告漏洞

不要在公开 Issue 中发布可利用的认证绕过、照片读取、邀请码、令牌或真实用户数据。请在仓库维护者提供的私密渠道中报告，并附上复现步骤、影响范围、受影响版本和临时缓解方式。没有私密渠道时，先提交不包含利用细节的 Issue，注明“安全问题”，等待维护者回复。

## 数据边界

- `JWT_SECRET`（至少 32 字节）、`APP_KEY`、SMTP/存储凭据和签名密钥只通过环境变量或 CI Secret 注入。
- 私密图片只能经 `/media/<id>` 鉴权代理读取；后台审核只返回 384px 缩略图。
- 邀请码、房间口令和房间 session token 不能写入后台列表、日志或错误响应。
- 原生 APK 可被逆向，任何编入 APK 的值都不能作为服务端信任根。

## 修复原则

修复安全问题时同时补回归测试；如果涉及数据库迁移、权限、照片或令牌，必须在 PR 中说明旧版本升级路径和回滚影响。
